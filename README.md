<div align="center">

# MMORPG Server Emulator

Эмулятор серверной части MMORPG, написанный на C# / .NET 10.

Исследовательский проект: попытка выяснить, насколько современный ИИ
способен разрабатывать и поддерживать эмулятор игрового сервера.

<sub>Неофициальный проект. Не связан с правообладателем игры. Все права на игру принадлежат ему.</sub>

</div>

---

## О проекте

Это эксперимент, а не продукт. Основная цель — не запустить игру, а
проверить на честной, достаточно сложной задаче, что ИИ умеет и чего не умеет:
реверс сетевого протокола, работа с бинарными пакетами, игровая логика,
состояние сотен одновременных сессий.

Эмулятор серверной части MMORPG — удачный полигон: здесь нет права на
«примерно правильно». Ошибка в смещении на один байт не падает сразу, а
тихо ломает протокол через сотню пакетов.

Проект развивается командой ИИ-агентов: планирование, разработка,
тестирование, ревью.

## Стек

| | |
|---|---|
| Платформа | .NET 10 (C#) |
| Данные | ADO.NET поверх хранимых процедур (`Server.Login`), Entity Framework Core 10 (`Server.Game`) |
| СУБД | MS SQL Server, SQLite |
| Логирование | Serilog |
| Хостинг | Microsoft.Extensions.Hosting |

## Структура

```
Servers/
  Core            сетевое ядро: сессии, шифрование, транспорт
  Server.Login    сервер авторизации          :11004
  Server.Game     игровой сервер              :11016
Packets/          структуры пакетов протокола
Databases/
  Database.Fnl    ADO.NET поверх хранимых процедур оригинальных баз FNL*
  Database.*      контексты EF Core: Account, Game, Parm, DataModel
Sites/            вспомогательные веб-сервисы (вне solution)
Utilities/        парсеры дампов, конвертеры, перевод lang-паков
```

## Сборка

Требуется **.NET SDK 10.0** или новее.

```bash
dotnet build Tested-Server.sln
```

Запуск серверов:

```bash
dotnet run --project Servers/Server.Login
```

```bash
dotnet run --project Servers/Server.Game
```

Адреса и порты задаются в `loginsettings.json` / `gamesettings.json`
соответствующих проектов.

### Доступ к базам

`Server.Login` берёт реквизиты **из DSN-файлов оригинального сервера** — тех же,
что читает он сам. Логин и пароль остаются в каталоге `Data` рядом с оригиналом
и в репозиторий не попадают: в конфигурации задаётся только путь.

Укажите каталог с `Account.dsn` и `Parm.dsn` — любым из способов:

```bash
FnlDatabase__DsnDirectory=/путь/к/CleanServer/Data dotnet run --project Servers/Server.Login
```

либо в нетрекаемом `appsettings.Local.json`:

```json
{ "FnlDatabase": { "DsnDirectory": "D:/CleanServer/Data" } }
```

Имя базы — это имя DSN-файла: `Account` → `Account.dsn` → `FNLAccount`.
Из файла читаются `Address`/`SERVER`, `DATABASE`, `UID`, `PWD`; ключ `DRIVER`
игнорируется — оригинал ходит через ODBC, эмулятор через TDS.
Если каталог не задан или файла нет, сервер не стартует и называет конкретный
недостающий файл.

Когда DSN-файлов нет (например, в контейнере), доступ можно задать напрямую —
строкой подключения в `ConnectionStrings:Account` / `ConnectionStrings:Parm`;
она перекрывает DSN.

`Server.Game` пока остаётся на самодельных базах через EF Core — его перевод
на оригинальную схему не сделан.

### Канал в контейнере

`Server.Login` собирается и запускается рядом с контейнером базы — см.
[Deploy/Channel](Deploy/Channel). Контейнер входит в сеть контейнера с базой, поэтому базы
доступны по имени контейнера, а DSN-файлов в образе нет: доступ задаётся строками подключения
из окружения.

Одна развилка стоит внимания. Канал ищет свою строку в `TblParmSvr` по адресу и слушает порт
из неё — а внутри контейнера адрес из таблицы принадлежит хосту и привязать его нельзя.
Поэтому адрес регистрации (`LoginSetting:ServerIp`) и адрес прослушивания
(`LoginSetting:BindIp`) разведены: в контейнере второй равен `0.0.0.0`, вне контейнера
остаётся пустым и совпадает с первым. Опубликованный порт обязан совпадать с `mTcpPort`
той же строки.

`Server.Game` в контейнер пока не заворачивался: развилка с адресом у него та же,
но отдельного параметра прослушивания у него ещё нет.

## Статус

Экспериментальный. Стабильность, полнота протокола и совместимость с
оригинальным клиентом не гарантируются.

Решение собирается целиком, без ошибок.

| Часть | Состояние |
|---|---|
| `Server.Login` | работает на оригинальных `FNLAccount` / `FNLParm`: авторизация, список серверов, выбор сервера |
| `Server.Game` | работает на оригинальных `FNLGame` / `FNLParm` / `FNLAccount`: экран персонажей, создание и удаление, вход в мир, перемещение с валидацией; остальная игровая логика портируется. Связь с каналом своего мира обязательна: без неё канал не покажет сервер в списке |
| `Site.Balance` | выведен из solution: ссылается на отсутствующий проект `Databases/Database.Balance` |

---

## Чеклист функциональности

Слева — подсистемы, которые есть в оригинальном сервере (состав восстановлен по
поведению серверной части и клиентскому протоколу). Справа — что из этого
воспроизведено здесь. Список ведётся как дорожная карта: он показывает и то, что
сделано, и то, насколько велика оставшаяся часть.

**Легенда:** ✅ работает · 🟡 частично · ⬜ не начато

### Инфраструктура

| Подсистема | Что делает | Статус | Где / чего не хватает |
|---|:--:|:--:|---|
| Приём соединений, реестр сессий | принимает TCP-клиентов, ведёт учёт и рассылку | ✅ | [NetworkServer.cs](Servers/Core/Network/NetworkServer.cs) |
| Асинхронный ввод-вывод сессии | неблокирующие приём и отправка, keep-alive | ✅ | [NetworkSession.cs](Servers/Core/Network/NetworkSession.cs) |
| Сборка пакетов из потока | режет поток на пакеты по префиксу длины | ✅ | реассемблер в [NetworkSession.cs](Servers/Core/Network/NetworkSession.cs): разорванные между TCP-сегментами кадры собираются, битая длина логируется и рвёт соединение |
| Диспетчеризация по коду пакета | ищет обработчик по коду, разбирает тело | ✅ | регистрация по атрибутам, [RegisterHandlerService.cs](Packets/Packets.Core/Services/RegisterHandlerService.cs) |
| Проверка целостности запроса | валидирует поля до обработки | 🟡 | вход на канал проверяется целиком, как в оригинале (индексы полей, длина ключа ресурсов, длины логина и пароля); создание/удаление персонажа, выбор и перемещение проверяются (состояние сессии, слот/класс/имя, NaN в координатах), запросы экипировки — тоже (вещь есть в сумке, подходит по уровню и классу, слоты совместимы), подбора и выброса — тоже (вещь лежит в мире и в пределах досягаемости, в сумке есть место, надетую вещь выбросить нельзя, слишком частые запросы отбрасываются); остальные пакеты принимаются на веру |
| Шифрование трафика | потоковый шифр на клиентском канале | 🟡 | [BlowfishCrypt.cs](Servers/Core/Network/Cryptography/BlowfishCrypt.cs) — расшифровка входящих; исходящие можно шифровать фича-флагом `EncryptOutgoingPackets` (по умолчанию выключен, живым клиентом не проверен). Гамма одинакова для каждого пакета — это совместимость с клиентом, а не защита трафика |
| Обмен ключом шифрования | выдаёт клиенту ключ сессии | 🟡 | по умолчанию заготовленный блок; генерация содержимого — фича-флаг `GenerateSessionKey` (живым клиентом не проверен, шифр на новый ключ не переключается) |
| Специализированные потоки | разделение сети, БД, таймеров и логов | 🟡 | фоновые задания вынесены на выделенные потоки планировщика; отдельных очередей БД и логов нет |
| Планировщик отложенных задач | единое «колесо» периодических и разовых задач | 🟡 | [PeriodicScheduler.cs](Servers/Server.Game/Services/Scheduling/PeriodicScheduler.cs): периодические задания с изоляцией ошибок и остановкой по токену хоста; разовых отложенных задач нет |
| Пул соединений с БД | по соединению на поток, несколько источников | ✅ | пулит сам ADO.NET; параметры пула настраиваются в секции `FnlDatabase`, неверные значения останавливают сервер на старте. Из шести источников оригинала подключены три (Account, Game, Parm) |
| Логирование | файл, консоль, уровни | ✅ | Serilog |
| Оперативный лог в БД | события сервера в таблицы логов | ⬜ | — |
| Конфигурация и фиксированная раскладка каталогов | настройки сервера, пути к данным | ✅ | `appsettings.json`, `loginsettings.json`, `gamesettings.json` |
| Запуск как системная служба | работа без сессии пользователя | ⬜ | — |
| Автотесты | регрессионная проверка | ⬜ | тестовых проектов в решении нет |

### Авторизация и вход в мир

| Подсистема | Что делает | Статус | Где / чего не хватает |
|---|:--:|:--:|---|
| Проверка учётной записи | сверяет логин и пароль в базе аккаунтов | ✅ | [AuthorizationHandler.cs](Servers/Server.Login/Core/Handlers/AuthorizationHandler.cs) — через оригинальную процедуру, проверки идут в порядке оригинала: состояние сессии → целостность запроса → сборка клиента → чёрный список адресов → мир учётной записи → сама учётная запись. Игрок видит первую сработавшую причину |
| Опция «проверять пароль» | берёт режим проверки из настроек сервера | ✅ | [ServersFactory.cs](Servers/Server.Login/Core/Factories/ServersFactory.cs) |
| Выдача сессионного ключа | генерирует и сохраняет ключ входа | ✅ | ключ пишется в учётную запись |
| Коды отказа клиенту | нет пользователя, неверный пароль, уже в игре | ✅ | причина не перечисляется, а считается так же, как в оригинале: код — CRC-32 от имени причины, и оно берётся прямо из ответа процедуры, поэтому работает любая причина, которой её когда-либо научили. Отказ 3102 несёт полный набор полей оригинала — причину, срок блокировки, её текст и признак вечной. Сверено с таблицей кодов клиента |
| Список игровых серверов | отдаёт клиенту серверы своего мира | ✅ | [ServersFactory.cs](Servers/Server.Login/Core/Factories/ServersFactory.cs) — список строится из реестра мира, как у оригинала: сервер без связи с каналом показан недоступным и из списка не исчезает, заполненность считается из числа игроков, о котором сервер сам сообщил, тип обслуживания и признак сервера Хаоса берутся из `TblParmSvr`. Байты пакета сверены с эталонной записью |
| Обновление списка по запросу | перечитывает список без переподключения | ✅ | [ServersHandler.cs](Servers/Server.Login/Core/Handlers/ServersHandler.cs) |
| Подтверждение входа по телефону | отвечает, нужна ли телефонная проверка на выбранном сервере | 🟡 | [ServersHandler.cs](Servers/Server.Login/Core/Handlers/ServersHandler.cs) — клиент шлёт этот запрос после выбора сервера в списке и до подключения к нему, поэтому он и выглядит как «сервер выбран». Сама телефонная проверка не делается (решение владельца): сервер всегда отвечает состоянием «не требуется» — ровно так же, как оригинал с выключенным флагом контента, и байты ответа сходятся с эталонной записью. Выбранный сервер сверяется с реестром мира, отказ уходит общим пакетом 1102 |
| Передача клиента в игровой мир | временный ключ, по которому игровой сервер пускает игрока | ✅ | логин-сервер пишет ключ сессии в базу аккаунтов, игровой сверяет его через оригинальную процедуру и ротирует при входе |
| Контроль повторного входа | не пускает второй раз и в другой мир | ✅ | вход в чужой мир отсекается процедурой мира и реестром соседей; канал ведёт свой список выданных ключей ([CertificationRegistry.cs](Servers/Server.Login/Services/Family/CertificationRegistry.cs)), второй вход того же аккаунта закрывает прежнюю сессию на канале, а если аккаунт уже в игре — канал отказывает и просит мир его выбросить. Ключ, за которым не пришли, перестаёт работать по таймеру |
| Уровни прав (GM, администратор) | ограничивает доступ к командам | ⬜ | — |
| Второй фактор (карта ключей) | выдача, проверка и отзыв таблицы кодов | ⬜ | не делаем, решение владельца |
| Одноразовые пароли | внешняя проверка входа | ⬜ | не делаем, решение владельца |
| Принудительное отключение игрока | кик по учётной записи или персонажу | 🟡 | кик по учётной записи идёт от канала к игровым серверам пакетом оригинала; игрок получает причину и отключается — [FamilyHandler.cs](Servers/Server.Game/Core/Handlers/FamilyHandler.cs). Кика по имени персонажа и команд администратора нет |
| Определение взлома учётной записи | реакция на подозрительный вход | ⬜ | — |

### Взаимодействие серверов (кластер)

| Подсистема | Что делает | Статус | Где / чего не хватает |
|---|:--:|:--:|---|
| Реестр серверов мира | кто из серверов на связи и какой у него тип | ✅ | [FamilyRegistry.cs](Servers/Server.Login/Services/Family/FamilyRegistry.cs) — состав мира читается из `TblParmSvr` теми же процедурами, что и у оригинала, остальное серверы заполняют сами по связи |
| Авторизация серверов друг у друга | сервер-сервер логин | ✅ | игровой сервер сам открывает связь к каналу своего мира ([FamilyLinkService.cs](Servers/Server.Game/Services/Family/FamilyLinkService.cs)), канал сверяет адрес подключения с `TblParmSvr` и отвечает своим номером; чужой номер и чужой адрес получают отказ |
| Поддержание связи, эхо-проверки | обнаружение отвалившегося соседа | 🟡 | пустой пинг по таймеру в обе стороны; разрыв виден сразу и снимает сервер со списка, но своего таймаута «молчит слишком долго» нет — связь держится, пока жив сокет |
| Публикация состояния и загрузки | свободен / обычный / занят / переполнен | 🟡 | сервер рассылает свои места по таймеру, канал пересчитывает их в четыре ступени заполненности для списка; шкала считается от числа сессий, пороги — в конфиге, на живом клиенте не проверялась |
| Уведомления о входе и выходе игроков | учёт онлайна на управляющем сервере | ⬜ | — |
| Зачистка зависших сессий и объектов | освобождение ресурсов после сбоя | ⬜ | — |
| Управляемая остановка сервиса | корректное завершение по команде | ⬜ | — |

### Справочники игровых данных

| Подсистема | Что делает | Статус | Где / чего не хватает |
|---|:--:|:--:|---|
| Загрузка справочников при старте | вычитывает игровые таблицы в память | 🟡 | [ParmRepository.cs](Servers/Server.Game/Services/Database/ParmRepository.cs) — читает оригинальную `FNLParm`; вычитывается лишь часть таблиц |
| Предметы и их свойства | характеристики, ограничения, наборы | 🟡 | данные читаются, применяются частично: в расчёте удара участвуют кубики урона, меткость и дальность оружия, защита и криты остальных вещей; учитываются требования вещи к уровню и классу владельца, её вклад в скорость удара, скорость движения и переносимый вес; виды оружия различают ближнее, дальнее, древковое и магическое. Наборы вещей, штрафы и бусины не применяются |
| Монстры, точки появления, добыча | что и где появляется, что выпадает | 🟡 | появление работает; из справочника берутся точки появления, вид поведения, радиус зрения и разброс блуждания; добыча тоже читается справочниками — группы монстра, связки внутри группы и строки вещей с количеством и состоянием |
| Опыт и уровни | таблицы роста | 🟡 | таблица читается, начисления нет |
| Умения и деревья умений | описания и зависимости | ⬜ | данные описаны, логики нет |
| Состояния (усиления и ослабления) | эффекты, сопротивления, правила снятия | ⬜ | данные описаны, логики нет |
| Экономика и торговые списки | цены, ассортимент лавок | ⬜ | — |
| Флаги контента | включение и отключение механик на лету | ⬜ | в оригинале почти каждая ветка начинается с такой проверки |

### Игровой мир

| Подсистема | Что делает | Статус | Где / чего не хватает |
|---|:--:|:--:|---|
| Список персонажей | отдаёт персонажей учётной записи | ✅ | [CharacterHandler.cs](Servers/Server.Game/Core/Handlers/CharacterHandler.cs) |
| Создание персонажа | лимит слотов, уникальность имени | 🟡 | охранные проверки в порядке оригинала, стартовые точки по классу из конфига; нет стартовых умений, коды ошибок приблизительные |
| Удаление персонажа | мягкое удаление с отметкой даты | ✅ | там же |
| Вход в мир | размещение персонажа, выдача его состояния | ✅ | [AuthorizationHandler.cs](Servers/Server.Game/Core/Handlers/AuthorizationHandler.cs) — последовательность оригинала с откатом при сбое |
| Выход из мира | корректное сохранение и освобождение | 🟡 | сохранение позиции и снятие отметки онлайна в базе работают; живым клиентом цепочка не прогонялась |
| Перемещение, прыжок, поворот | движение персонажа и рассылка соседям | 🟡 | [CharacterActionHandler.cs](Servers/Server.Game/Core/Handlers/CharacterActionHandler.cs) + [MoveSystem.cs](Servers/Server.Game/Core/Systems/MoveSystem.cs): валидация дистанции и порогов, откат с ресинком, подтверждение уходит и соседям, и самому инициатору; детектора скорости и карт коллизий нет |
| Видимость объектов | кто кого видит: игроки, монстры, предметы | 🟡 | [VisibleGameService.cs](Servers/Server.Game/Services/Game/VisibleGameService.cs); ошибка разбиения на пачки |
| Чат | локальный, приватный, торговый, общий | 🟡 | четыре канала из полутора десятков; команды администратора отключены |
| Эмоции | анимации-жесты | ✅ | [ChatHandler-.cs](Servers/Server.Game/Core/Handlers/ChatHandler-.cs) |
| Подбор и выброс предметов | перенос между землёй и сумкой, вес, стопки | 🟡 | [InventoryHandler.cs](Servers/Server.Game/Core/Handlers/InventoryHandler.cs) — подобранная вещь получает настоящий серийный номер и собственную строку в базе: строку создают процедуры оригинала, они же складывают стопки по его правилам и возвращают серийник той записи, в которую слили. Поднятую вещь можно сразу надеть. Выброс переносит строку вещи служебному владельцу, под которым у оригинала живут вещи на земле, и кладёт вещь рядом с игроком с разбросом; надетую вещь выбросить нельзя. Порядок проверок — как у оригинала, игрок видит первую сработавшую причину: мёртвый ничего не поднимает, вещь должна лежать в мире и быть в пределах досягаемости по плоскости и по высоте, в сумке нужны свободная строка и запас до потолка стопки. Любой отказ отвечает пакетом ошибки с настоящим кодом — клиент показывает его текстом. Вес сумки считается по правилам оригинала, состояния «нормально / тяжело / перегруз» пересчитываются от предела персонажа, а пакет веса уходит только тогда, когда вещь что-то весит. Двойной подбор закрыт атомарным захватом вещи: она достаётся тому, кто успел первым, второму отвечают, что вещи уже нет. Слишком частые запросы отбрасываются молча — как у оригинала. Неопознанная вещь и на земле, и в сумке, и при входе в мир показывается подменным номером. Резерва добычи за убийцей и партийной добычи нет, поле владельца резерва уходит со значением «никому» |
| Использование предметов | зелья, еда, расходники | ⬜ | обработчик отвечает отказом |
| Экипировка и снятие | надеть, снять, пересчитать характеристики | 🟡 | [EquipHandler.cs](Servers/Server.Game/Core/Handlers/EquipHandler.cs) + [GEquipChange.cs](Servers/Server.Game/Models/Game/Player/GEquipChange.cs): вещь надевается и снимается прямо в игре — слот выбирает сервер по виду вещи, занятый слот меняется одним запросом, кольца ложатся по двум слотам, снятое оружие дальнего боя уносит с собой и стрелы. Перед сменой проверяются состояние персонажа (мёртвый не переодевается), наличие вещи в сумке, её требования к уровню и классу, совместимость слотов — стрелы без лука не надеваются, копьё не уживается со щитом. Изменение сначала пишется в базу и только потом применяется: неудачная запись откатывается, и операция отменяется целиком. Дальше — полный пересчёт характеристик, скоростей и переносимого веса, пакеты самому игроку и соседям, которые видят новую внешность; книга и посох надеваются как оружие (слот магического оружия оригиналом не подтверждён), дистанция удара считается от оружия в руке. Запись идёт живыми процедурами оригинала построчно — по строке на слот, — поэтому слоты материалов переживают перезаход. Требование вещи к уровню читается со знаком: вещь, написанную для низких уровней, переросший её персонаж не наденет. Отказ отвечает пакетом ошибки с настоящим кодом, который клиент показывает текстом, а проверки идут в порядке оригинала — игрок видит первую сработавшую причину. Проклятые вещи, печати и поломка слотов, наборы вещей и сервант — нет |
| Бой | удар, урон, промах, смерть | 🟡 | цикл PvE в обе стороны: автоатака по монстру и ответный удар монстра одним и тем же расчётом, попадание/промах/крит, урон кубиками оружия по характеристикам обеих сторон, здоровье цели в пакете — до удара; два канала урона по типу оружия в руке — [AttackSystem.cs](Servers/Server.Game/Core/Systems/AttackSystem.cs) + [AttackGameService.cs](Servers/Server.Game/Services/Game/AttackGameService.cs), подробности модели удара — в комментариях там же. Своё здоровье игрок узнаёт отдельным пакетом, а падение до нуля убивает персонажа с оповещением соседей — [PlayerDeathSystem.cs](Servers/Server.Game/Core/Systems/PlayerDeathSystem.cs). Дистанция удара берётся от оружия в руке — лук бьёт со своей дальности, — а смена оружия посреди боя подхватывается следующим ударом, без перезахода. Нет бонусов против расы, стихийного урона, мгновенных модификаторов, PvP-меткости, скилловых атак, самого PvP и сейф-зон; отдельного магического канала у удара нет, стрелы для выстрела не требуются и не расходуются. Тайминги ударов и отображение урона живым клиентом не проверены |
| Возрождение | подъём после смерти | 🟡 | подъём в домашней точке или стартовой точке класса с оповещением соседей и ресинхронизацией, поднимается только мёртвый персонаж — [CharacterActionHandler.cs](Servers/Server.Game/Core/Handlers/CharacterActionHandler.cs); путь «монстр убил — игрок возродился» проходит целиком, живым клиентом не проверялся. Автоматического подъёма по таймеру и штрафа за смерть нет |
| Опыт и уровни | начисление, повышение уровня | 🟡 | [ExpSystem.cs](Servers/Server.Game/Core/Systems/ExpSystem.cs): опыт за убийство, перенос излишка, несколько уровней за раз, пересчёт характеристик; опыт уходит раньше пакета смерти. Штраф за смерть не подключён |
| Восстановление здоровья и маны | пассивная регенерация | ✅ | [RecoveryGameService.cs](Servers/Server.Game/Services/Game/RecoveryGameService.cs) — игроки и монстры, период из конфигурации |
| Состояния (усиления и ослабления) | наложение, истечение, снятие | ⬜ | — |
| Умения | применение, откаты, наборы умений | ⬜ | — |
| Улучшение предметов | заточка с шансом успеха | ⬜ | — |
| Появление монстров | возрождение по точкам появления | ✅ | [UnitGameService.cs](Servers/Server.Game/Services/Game/UnitGameService.cs) — монстр возрождается на своей точке появления и без памяти о прошлом бое; пакеты появления несут его настоящее состояние (мёртв, стоит, идёт, зол) и точку, куда он идёт — [VisibleFactory.cs](Servers/Server.Game/Core/Factories/VisibleFactory.cs) |
| Поведение монстров | преследование, атака, возврат | 🟡 | [MonsterAiGameService.cs](Servers/Server.Game/Services/Game/MonsterAiGameService.cs): монстр отвечает на удар — помнит всех, кто его бил, держится за цель и берёт следующего обидчика, когда прежний пропал; догоняет бегом, бьёт в упор, бросает бесплодную погоню, уходит домой по поводку от точки появления и добирает здоровье обычной регенерацией. Активные виды сами берут ближайшую цель в радиусе зрения из справочника, но удар настоящего обидчика перебивает цель «по зрению»; без цели монстр прогулочным шагом блуждает вокруг своей точки — шаги и остановки в [MonsterMoveSystem.cs](Servers/Server.Game/Core/Systems/MonsterMoveSystem.cs). Движение — по прямой: обхода препятствий и навигационных путей нет. Нет групповой агрессии, сейф-зон и особых видов ИИ (дальнобойные, охранники, боссы); тайминги живым клиентом не проверены |
| Выпадение предметов | добыча с побеждённых монстров | 🟡 | [UnitDropSystem.cs](Servers/Server.Game/Core/Systems/UnitDropSystem.cs) — что оставит убитый монстр, решают справочники оригинала тремя уровнями броска: сначала группы добычи монстра, затем каждая связка вещей внутри выбранной группы, а вещи самой связки выпадают уже без броска, с количеством и состоянием из её строки. Вещь либо ложится на землю с разбросом вокруг трупа, либо уходит сразу в сумку убийце — это свойство группы; деньги выпадают обычной вещью, со своим разбросом количества. Вещи появляются перед пакетом смерти монстра, в порядке оригинала, и показываются соседям обычным проходом видимости. Лежащая на земле добыча живёт в памяти без серийного номера — строку в базе создаёт подбор, так что убийство монстра в базу не ходит. Нет резерва добычи за убийцей и партийной добычи, квестовой добычи, множителей добычи (места карты, глобальных, премиум), налога осадного замка и «желудка» монстра; проходимость точки, куда ложится вещь, не проверяется |
| Диалоги с персонажами мира | скрипты, меню, действия | ⬜ | обработчик отвечает ошибкой |
| Торговля с лавками | покупка и продажа | ⬜ | обработчик отвечает ошибкой |
| Сохранение состояния | периодическая запись в базу | 🟡 | [GameSaveService.cs](Servers/Server.Game/Services/Database/GameSaveService.cs) — периодически сохраняются только координаты; смена экипировки, подбор и выброс вещи пишутся в базу сразу в момент операции, мимо этой службы |
| Сбор мусора мира | уборка брошенных предметов и трупов | ✅ | [GarbageGameService.cs](Servers/Server.Game/Services/Game/GarbageGameService.cs) — вещь лежит на земле десять минут, как в оригинале, и уходит и из мира, и из базы: у выброшенной игроком вещи удаляется её строка, у добычи с монстра удалять нечего. Сокращение времени жизни, когда вещей в мире накопилось слишком много, не воспроизводится |

### Социальные и групповые системы

| Подсистема | Что делает | Статус |
|---|:--:|:--:|
| Задания | выдача, прогресс, награда | ⬜ |
| Группа | совместная игра, общий опыт и добыча | ⬜ |
| Гильдии | состав, права, склад, умения гильдии | ⬜ |
| Друзья | список, статус в сети | ⬜ |
| Обмен между игроками | безопасная передача вещей | ⬜ |
| Личные лавки | торговля между игроками | ⬜ |
| Спутники и питомцы | вызов, развитие, снаряжение | ⬜ |
| Телепорты | точки перемещения по миру | ⬜ |
| Осады и массовые режимы | арена, командные бои, война гильдий | ⬜ |
| Объявления сервера | оповещения всем игрокам | ⬜ |

### Внешние и региональные подсистемы

| Подсистема | Что делает | Статус |
|---|:--:|:--:|
| Учёт оплаты и игровых клубов | определение платного подключения по адресу | ⬜ |
| Региональные проверки | требования отдельных регионов к учётным записям | ⬜ |
| Почтовые уведомления | письма по учётной записи | ⬜ |
| Защита от автоматизации | выявление посторонних программ у клиента | ⬜ |

### Сводка

| Блок | ✅ | 🟡 | ⬜ | Всего |
|---|:--:|:--:|:--:|:--:|
| Инфраструктура | 7 | 5 | 3 | 15 |
| Авторизация и вход в мир | 6 | 3 | 5 | 14 |
| Взаимодействие серверов | 0 | 0 | 7 | 7 |
| Справочники | 0 | 4 | 4 | 8 |
| Игровой мир | 7 | 13 | 6 | 26 |
| Социальные системы | 0 | 0 | 10 | 10 |
| Внешние подсистемы | 0 | 0 | 4 | 4 |
| **Итого** | **20** | **25** | **39** | **84** |

Что сегодня действительно проходит от начала до конца: подключение к
логин-серверу, вход по настоящей учётной записи, получение списка серверов,
выбор сервера и передача сессии игровому серверу. В игровом мире — экран
персонажей, создание и удаление с охранными проверками, вход в мир по
последовательности оригинала, перемещение с валидацией дистанции, чат,
видимость окружения, появление монстров, а также
боевой цикл: автоатака по монстру, урон, смерть, опыт и уровни,
регенерация здоровья и маны. Удар считается по характеристикам обеих сторон и
кубикам оружия. Экипировку можно менять на ходу: вещи надеваются и снимаются с
проверками, изменение сразу уходит в базу, характеристики и скорости
пересчитываются, соседи видят новую внешность, а следующий удар уже наносится
новым оружием и с его дистанции. Замкнут и круг вещей: убитый монстр роняет
добычу по справочникам — на землю с разбросом вокруг трупа или сразу в сумку
убийце, деньгами в том числе, — подобранная вещь получает настоящий серийный
номер и строку в базе, складывается в стопку по правилам оригинала и тут же
надевается, выброшенная ложится на землю и через десять минут исчезает вместе
со своей строкой; надетую вещь выбросить нельзя, а любой отказ приходит
пакетом ошибки, который клиент показывает текстом. Резерва добычи за убийцей,
партийной и квестовой добычи, множителей добычи и использования предметов
нет. Монстры живут своей жизнью: блуждают у точки
появления, активные виды сами выбирают жертву поблизости, отвечают на удар,
преследуют обидчика и возвращаются домой, когда погоня не удалась; монстр может
убить персонажа, после чего игрок возрождается. Монстры ходят по прямой, без
обхода препятствий; PvP, умений и штрафа за смерть нет.

---

## Правовая информация

Оригинальная игра и все связанные с ней торговые марки, названия, графика,
игровые данные и иные материалы являются собственностью её правообладателя.

Этот проект:

- разработан **независимо** и **не аффилирован** с правообладателем игры;
- **не одобрен**, не спонсируется и никак иначе не поддерживается правообладателем;
- **не распространяет** игровой клиент, оригинальные серверные исполняемые
  файлы, игровые ресурсы, дампы трафика, результаты дизассемблирования и
  иные проприетарные или производные от них материалы;
- создан исключительно в **исследовательских и образовательных** целях;
- **не предназначен** для коммерческого использования и не преследует цели
  извлечения прибыли или нанесения ущерба правообладателю.

Репозиторий содержит только оригинальный исходный код на C#. Материалы,
производные от проприетарных файлов правообладателя, в него не включены.

Все права на оригинальную игру принадлежат её правообладателю. Если вы являетесь
правообладателем и считаете, что этот репозиторий нарушает ваши права,
свяжитесь с владельцем репозитория — материалы будут удалены.

---

<div align="center">

Made with ❤️ for R2Genius community

</div>
